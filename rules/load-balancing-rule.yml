apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: spring-boot-istio-routing-service
spec:
  destination:
    name: spring-boot-istio-routing-service
  route:
    - labels:
        version: a
      weight: 100
    - labels:
        version: b
      weight: 0
        