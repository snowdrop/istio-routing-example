apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    version: a
    app: my-service
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: my-service